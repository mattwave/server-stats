# server-stats
Server Stats Shell Script

How to use it

    Create the file:
    nano server-stats.sh

    Paste the code above into the editor and save (Ctrl+O, Enter, Ctrl+X).

    Make it executable:
    chmod +x server-stats.sh

    Run it:
    ./server-stats.sh
